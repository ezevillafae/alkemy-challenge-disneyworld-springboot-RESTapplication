# Connection url for the database "disneydb"
spring.datasource.url = jdbc:mysql://localhost:3306/disneydb

# Username and password
spring.datasource.username = root
spring.datasource.password =
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================

# Show or not log for each sql query
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql=true
# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update


# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Security
disney.app.security.jwtSecret=secret
disney.app.security.jwtExpirationMs=999999

# Emails
disney.app.email.sender=eze.villafae07@gmail.com
disney.app.email.enabled=true
spring.main.allow-circular-references=true

